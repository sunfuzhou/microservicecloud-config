---
spring:
  profiles: dev
  application:
    sex: man
    name: microservicecloud-config-atguigu-dev
    age: 48
---

spring:
  profiles: test
  application:
     name: microservicecloud-config-atguigu-test
     #必须保存为UTF-8格式
